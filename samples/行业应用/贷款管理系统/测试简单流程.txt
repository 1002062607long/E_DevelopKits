A.运行本系统后,弹出提示窗口,里面有一个例子数据,提示默认的用户名与口令为admin.并提示要及时更换口令,
在登录窗口中填写示默认的用户名与口令为admin,进入主窗口后,可对例子数据进行操作,可以通过登录为普通操作员进行操作.

B.正式使用前先进行初始化,再移植数据,移植数据时为空(菜单是"贷款数据移值"->"重新生成空表")或有数据时均能移植成功,
重新启动后进行开工处理.

C.以系统管理员进入,进行帐务处理.

D.以普通操作员进入,进行业务处理.

另:恢复测试状态是将DEMO目录中的数据库拷贝到主程序目录下即可.