《易容大师》视频教程之播放器篇

先从易向导执行易容大师，这样易容大师可以直接向易语言编程环境自动创建界面。

将设计好的界面图片逐一拖入，当然也可以直接从“编辑”菜单导入。

观察这些图片之间，在有按钮和滑块的地方会有些差异，这就是鼠标经过或按下时要表现的效果。

放大以便于绘制控件矩形。

绘制完毕，可以使用光标键微调位置，使用Shift和光标键微调大小。

给控件矩形添加备注，这样《易容大师》在自动创建控件时会根据这些备注给控件命名。

设置水平滑块。

注意这里滑条标记为6。

先要生成皮肤图片和配置文件。

再根据皮肤皮肤配置文件(*.esk_)生成独立的皮肤文件(*.esk)。

自动创建界面控件。

呵呵，控件已经自动创建好并摆放好位置。

注意易容大师模块已事先安装好。

皮肤已经穿上了，不过还有点小问题，我们需要修改一下图片和控件的次序。

观察这里，控件根据备注自动命名。

在Photoshop 中简单处理一下切割后的图片，特别是有滑块条的地方。这些都是很简单的操作，仅仅是把一块颜色覆盖另一块颜色。

图片修改完毕，再重新由皮肤配置文件(*.esk_)生成独立的皮肤文件(*.esk)。可以直接拖入皮肤配置文件，也可以从菜单选择。

再运行一下程序，看，已经都好了。 :>

再制作另一套皮肤，过程类似。

选中控件矩形并绘制以重新定位其位置和大小。

OK,换肤成功，使用《易容大师》，一切就这么简单。 *^_^#

教程到此结束，谢谢观赏。

隐藏控件矩形列表框，以获得更大的工作空间。

先用辅助线勾勒出主要的控件边界。
按键盘上的＋键同时插入水平和垂直辅助线。

辅助线的颜色和背景色太接近，这里设置一下。

这四根辅助线圈出主控件。

按键盘上的|键插入一根垂直辅助线。

继续插入辅助线把控件都划分出来。

记得要添加边界辅助线，这样绘制在边界上的控件矩形才会自动捕捉到边界上。

可以看到，
绘制完毕的控件矩形会自动捕捉到辅助线上。

在绘制控件矩形的时候，大脑中要有清晰的概念：哪些区域需要能够自动缩放以适应窗口大小的改变，该区域如果需要延伸，则该区域的图片应该是可以平铺而没有接痕的。

为了看得更清楚，更改一下控件矩形的显示方式。

对于未自动捕捉到辅助线的控件矩形，可以先使用键盘微调，然后单击此菜单项使其捕捉到辅助线上。

开始设置控件矩形的类型和锚点的属性。

此为标题栏，要显示文字，所以选择“标签”类型。

设置窗口为可调整大小。
否则不能设置控件的锚点属性。

标题栏要自动扩展，所以要锚住右边。

这三个按钮因为要随窗口的大小变化而向右移动，所以要锚住右边，不锚住左边。

这个也要自动扩展填充，所以既要锚住左边，也要锚住右边，不然窗口被拉大时就露馅了。:P

要使用非贴图控件时，这里要选择“其他”类型。

状态栏也可以使用“标签”类型。

给重要的控件矩形加上备注。

注意这里要设置成右下角那个控件矩形的标记，这样可以通过拖动它来改变窗口的大小。

因为自定义的控件多种多样，所以不能使用标记作为标识，需要指定控件数值，这样易容大师才能够找出它。(易语言的“取标记控件”函数只能取预先知道类型的控件。)

好了，可以看到，控件能够自动相应窗口大小的改变，但有的地方还是“露馅”了。 :P

替换控件，
位置和大小不需要十分精确，
易容大师会自动调整。

替换控件后，要更改相应的代码。

标题栏的文字发生相应的改变，超文本浏览框也打开了正确的页面。剩下的代码就交给你来写了。:D

这个例子过程有些复杂，请多练习几次，自会熟能生巧。