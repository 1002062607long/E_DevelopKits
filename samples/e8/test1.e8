
; -------------------------------------------------------------
; 本节用作定义对应易语言程序的一些全局设置信息

[settings]

; 提供本e8文件内所包含的一个易语言程序文件(.e)/易语言模块文件(.ec)/易语言文本代码文件名(.txt或其它后缀),如果不是绝对路径,则认为是相对于本e8文件所处目录的相对路径.
; 可以同时定义多个"source"属性,以同时提供多个文件名,系统将会自动将这些文件合并到一起后打开,重复的程序启动代码将会被自动滤除(具体为"_启动窗口"及其程序集,"_启动子程序").
; 注意: 只要提供了一个易语言模块文件(.ec),那么所有在此处列出的易语言程序文件(.e)中的原有易语言模块文件在合并前都会被首先清除.
source = e8test.e

; -------------------------------------------------------------
; 本节用作定义对应易语言程序的程序设置信息

[AppSettings]

; 用作设置本程序的类型,可以为以下值之一:  WinEXE, WinConsoleEXE, WinDLL, WinECom, LinuxConsoleELF, LinuxECom
type = WinEXE

name = "E8测试程序1"
explain = "本程序用作测试E8易语言程序配置信息机制"
author = "飞扬软件工作室"
ZipCode = "443200"
address = "湖北"
photo = ""
fax = ""
email = "wutao@vip.163.com"
HomePage = "www.vg3d.com"
copyright = "飞扬软件工作室 版权所有"
MajorVersion = 1
MinorVersion = 0
BuildNO1 = 1
BuildNO2 = 0
plugins = ""
EnableExportPublicMethod = FALSE
icon = "test.ico"

; 提供编译输出文件名.如果不是绝对路径,则认为是相对于本e8文件所处目录的相对路径.
OutputFileName = "e8test1.exe"

; -------------------------------------------------------------
; 本节用作定义所指定易语言程序内的常量值,当打开e8文件时,如果所指定名称常量不存在,则自动创建一个加入易语言程序,如果存在,则将覆盖其值.
; 在此处所设置或修改的常量,其注释首部会有"E8"标注.
; 由于易语言编译器的所有流程控制命令都有针对常量条件值的特殊处理(即如果条件值是常量,会自动跳过为假的代码部分不编译),因此利用在此处所设置的宏可以达到C语言的条件编译效果.

[user_consts]

; 定义一个数值型常量
类型 = 1

; 定义一个日期时间型常量
日期时间 = [2015/7/20]

; 定义一个文本型常量
作者 = "飞扬软件工作室"

; 定义一个逻辑型常量
显示信息 = 假
